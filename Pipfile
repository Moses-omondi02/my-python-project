[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
sqlalchemy = ">=2.0.0"
click = ">=8.1.0"
python-dotenv = ">=1.0.0"
tabulate = ">=0.9.0"
alembic = ">=1.8.0"

[dev-packages]
pytest = ">=7.0.0"

[requires]
python_version = "3.9"